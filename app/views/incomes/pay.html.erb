<%- model_class = Income -%>
<div class="page-header">
  <h1><%=t '.title', :default => [:'helpers.titles.pay', 'Pay %{model}'], :model => model_class.model_name.human.titleize %></h1>
</div>

<%= render 'readonly_form', income: @income %>

<%= render 'costs/form', cost: @cost %>